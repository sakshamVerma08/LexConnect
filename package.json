{
  "name": "lexconnect",
  "version": "1.0.0",
  "description": "LexConnect - Legal Services Marketplace",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm run build"
  },
  "engines": {
    "node": ">=20.18.0 <21.0.0",
    "npm": ">=10.5.0"
  }
}
